---
import Layout from "../../layouts/Layout.astro";

// TODO: Get user role from authentication
const userRole = "admin";

// Redirect non-authorized users to shows list
if (userRole !== "department_representative" && userRole !== "admin") {
  return Astro.redirect("/shows");
}

import ShowCreator from "../../components/shows/ShowCreator.tsx";
---

<Layout title="Tworzenie nowej wystawy">
  <ShowCreator client:load />
</Layout>
